# k6 benchmark

This folder is reserved for a k6 scenario that exercises the same
`01-inefficient` API and disposable benchmark database as the Crank scenario.

Do not create another API project here. Add the k6 script and its runner when
the k6 benchmark is implemented.
