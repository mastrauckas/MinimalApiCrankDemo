name: minimal-api-performance-demo

services:
  sqlserver:
    image: mcr.microsoft.com/mssql/server:2022-latest
    container_name: performancedemo-sqlserver
    environment:
      ACCEPT_EULA: "Y"
      MSSQL_PID: Developer
      MSSQL_SA_PASSWORD: ${MSSQL_SA_PASSWORD}
      SQLSERVER_DATABASE: PerformanceDemo
      SQLSERVER_USER: sa
    ports:
      - "14333:1433"
    volumes:
      - performancedemo-sqlserver-data:/var/opt/mssql
      - ./seed:/seed:ro
    healthcheck:
      test:
        - CMD-SHELL
        - >-
          /opt/mssql-tools18/bin/sqlcmd -S localhost -U sa
          -P "$${MSSQL_SA_PASSWORD}" -C -Q "SELECT 1" || exit 1
      interval: 5s
      timeout: 3s
      retries: 30
      start_period: 20s

volumes:
  performancedemo-sqlserver-data:
    name: performancedemo-sqlserver-data
